# Node REPL

## What It Does

`node_repl` is a persistent Node.js execution environment exposed as MCP.

## Best Uses

- Quick JavaScript experiments
- JSON/data manipulation
- Running npm packages in-repl
- Browser plugin support when the browser skill wants it

## Rule

- Use it when shell is clumsy and JavaScript is the cleanest path.
- It is not a general web search tool.
